Added ``psynet deploy ssh --ingress`` for optional per-app Cloudflare tunnels, ``psynet hibernate ssh`` / ``psynet awaken ssh``, automatic wake-before-export, and ``--use-local-dallinger`` to bake an unreleased Dallinger checkout into the experiment image. PsyNet records ``kind=psynet`` monitoring metadata in the Dallinger deployment manifest.
