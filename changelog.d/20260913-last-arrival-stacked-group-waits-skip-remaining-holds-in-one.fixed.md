Last-arrival stacked group waits skip remaining holds in one request. Last-arrival waits for the barrier-instance advisory claim so it does not first-paint a hold while the poller owns the visit; if the poller already skipped, that GET follows the live cursor. Reconstructed trial holds with the same hold id are the same wait, not a cursor move. If a waiter row is busy, last-arrival retries waiter ``NOWAIT`` immediately; the 0.5 s poller finishes the stacked skip in one sweep and publishes wakes after that walk.
