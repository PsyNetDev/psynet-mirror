Unfilled waiter arrivals try the visit claim without waiting unless that check would release partners, including ``POST /response`` and checks queued after a ready-hold skip. Spec errors during that peek leave the group waiting. Automated drivers POST hold-resume, pause on still-waiting overlays, and retry busy 503s plus JSON 409 stale timeline GETs, so parallel bots no longer stall last-arrival skip or fail when a partner advances them mid-render.
