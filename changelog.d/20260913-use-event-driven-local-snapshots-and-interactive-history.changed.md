Changed local deployment snapshots to be event-driven only (participant
finish, shutdown when needed, and next-launch recovery), removing the
ten-minute periodic snapshot timer, and made ``psynet history`` open an
interactive Rich Live browser by default in a terminal.
