Playwright stacked-group hold overlays compare wake→end wallclock with ``max(2200ms, hold-resume Server-Timing app + 500ms)``. Summaries print gunicorn ``queue~`` so a long wait can be split into handler time versus pool occupancy; that queue is not subtracted from linger or waiter-release spread.
