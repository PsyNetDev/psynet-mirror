Playwright stacked-group hold overlays compare wake→end wallclock with ``max(2500ms, hold-resume Server-Timing app + 800ms)``. Waiter-release spread is at most 2200ms. ``GET /timeline`` and load-participant blocking checks use a 3000ms handler budget. Summaries print gunicorn ``queue~`` so a long wait can be split into handler time versus pool occupancy; that queue is not subtracted from linger or waiter-release spread.
