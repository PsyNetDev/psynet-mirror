The documentation, the adaptive-experiment Agent Skill, and the `simple_rating`, `similarity`, `step_tag`, `timed_push_buttons`, `simple_audio_rating`, `audio_similarity`, and `audio_stimulus_set_from_dir` demos now recommend keeping ready-made stimulus files in the experiment's `static/` folder and referring to them by URL (for example with `psynet.media.static_url_for`), rather than registering them as assets.
