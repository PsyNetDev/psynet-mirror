Added a shared deployment event history for live deploy, sandbox, export, and
destroy commands, including optional ``--comment``, free-floating
``psynet comment`` notes, full command ``argv`` capture, failure reasons, and
a Rich ``psynet history`` viewer for ``data/deployment-events.jsonl``.
