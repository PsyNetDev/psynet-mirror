Added required IDs, recovery snapshots, and deployment event history for local live experiments. Snapshots are written when a participant finishes, on shutdown when needed, before resetting interrupted managed state, and every ten minutes when the response table has grown. A local launch now refuses to reset the shared local database while another local experiment is still running, and says which one to stop.
